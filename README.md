# learn-academic

Landing page concept for a math & IT learning platform that includes courses, videos, exercises, and a Pro subscription tier.

## Preview
Open `index.html` directly in a browser, or run a quick local server:

```bash
python3 -m http.server 8000
```

Then visit `http://localhost:8000`.
